import { FilterChannelPipe } from './filter-channel.pipe';

describe('FilterChannelPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterChannelPipe();
    expect(pipe).toBeTruthy();
  });
});
